import { View, Text } from 'react-native';
import RenderReservas from "../../components/reservation/reservas";

const Reservas = () => {
  return (
    <RenderReservas />
  );
};

export default Reservas;